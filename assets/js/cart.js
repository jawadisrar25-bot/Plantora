function renderCart(){const c=getCart(),host=document.getElementById('cartItems');if(!c.length){host.innerHTML='<div class="empty"><h3>Your cart is empty</h3><p>Browse the Plantora collection and add your preferred plants.</p><a class="btn" href="shop.html">Shop Plants</a></div>';document.getElementById('cartSummary').innerHTML='';return}host.innerHTML=c.map(x=>{const p=P.find(y=>y.id===x.id);return `<div class="cart-item"><img src="${p.image}" alt="${p.name}"><div><div class="product-kicker">ITEM #${p.itemNo}</div><h3>${p.name}</h3><div>${p.size} · ${money(p.price)}</div><div class="qty"><button onclick="changeQty('${p.id}',-1)">−</button><span>${x.qty}</span><button onclick="changeQty('${p.id}',1)">+</button></div></div><div><strong>${money(p.price*x.qty)}</strong><br><button class="icon-btn" onclick="removeItem('${p.id}')">Remove</button></div></div>`}).join('');const subtotal=c.reduce((s,x)=>{const p=P.find(y=>y.id===x.id);return s+(p.price||0)*x.qty},0);document.getElementById('cartSummary').innerHTML=`<div class="summary-card"><h3>Order Summary</h3><div class="summary-row"><span>Subtotal</span><strong>${money(subtotal)}</strong></div><div class="summary-row"><span>Delivery</span><span>Confirmed after order</span></div><div class="summary-row total"><span>Total</span><span>${money(subtotal)}</span></div><a class="btn" style="width:100%;margin-top:12px" href="checkout.html">Proceed to Checkout</a></div>`}
function changeQty(id,d){const c=getCart(),e=c.find(x=>x.id===id);if(!e)return;e.qty+=d;if(e.qty<1)return removeItem(id);saveCart(c);renderCart()}function removeItem(id){saveCart(getCart().filter(x=>x.id!==id));renderCart()}document.addEventListener('DOMContentLoaded',renderCart);

document.addEventListener('DOMContentLoaded',()=>{const grid=document.getElementById('productGrid'),search=document.getElementById('search'),sort=document.getElementById('sort'),cat=document.getElementById('category');const cats=['All',...new Set(P.map(x=>x.category))];cat.innerHTML=cats.map(c=>`<option value="${c}">${c}</option>`).join('');const q=new URLSearchParams(location.search);if(q.get('category'))cat.value=q.get('category');function draw(){let a=[...P];const s=search.value.toLowerCase().trim();if(s)a=a.filter(x=>(x.name+x.itemNo+x.category+x.size).toLowerCase().includes(s));if(cat.value&&cat.value!=='All')a=a.filter(x=>x.category===cat.value);if(sort.value==='price-asc')a.sort((a,b)=>(a.price??999999)-(b.price??999999));if(sort.value==='price-desc')a.sort((a,b)=>(b.price??-1)-(a.price??-1));if(sort.value==='size')a.sort((a,b)=>parseFloat(a.size)-parseFloat(b.size));grid.innerHTML=a.length?a.map(card).join(''):`<div class="empty">No products found.</div>`;document.getElementById('resultCount').textContent=`${a.length} products`;};[search,sort,cat].forEach(e=>e.addEventListener('input',draw));draw()});

document.addEventListener('DOMContentLoaded',()=>{const id=new URLSearchParams(location.search).get('id');const p=P.find(x=>x.id===id)||P[0];if(!p)return;document.title=`${p.name} | Plantora`;document.getElementById('productDetail').innerHTML=`<div class="detail-image"><img src="${p.image}" alt="${p.name}"></div><div class="detail-content"><div class="eyebrow">ITEM #${p.itemNo}</div><h1>${p.name}</h1><div class="detail-price">${money(p.price)}</div><div class="chips"><span class="chip">Size: ${p.size}</span><span class="chip">${p.category}</span><span class="chip">Artificial Plant</span></div><p>${p.description}</p><p>Designed for low-maintenance interiors, this Plantora piece adds greenery and presence without ongoing plant care.</p>${p.price==null?`<div class="notice">Price is not shown in the supplied product image. Please contact Plantora for the current price.</div>`:''}<div style="display:flex;gap:10px;margin-top:22px;flex-wrap:wrap"><button class="btn" onclick="addToCart('${p.id}')">${p.price==null?'Request Price':'Add to Cart'}</button><a class="btn secondary" href="https://wa.me/966548872490?text=${encodeURIComponent('Hello Plantora, I am interested in Item #'+p.itemNo+' ('+p.size+').')}" target="_blank">WhatsApp</a></div></div>`;const r=P.filter(x=>x.id!==p.id&&x.category===p.category).slice(0,4);document.getElementById('related').innerHTML=r.map(card).join('')});
